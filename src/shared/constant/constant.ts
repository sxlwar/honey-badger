export enum CRUDVar {
    GET = 'get',
    CREATE = 'create',
    UPDATE = 'update',
    DELETE = 'delete',
    SEARCH = 'search',
}

export const API = 'api';

export enum Env {
    production = 'production',
    development = 'development',
}
